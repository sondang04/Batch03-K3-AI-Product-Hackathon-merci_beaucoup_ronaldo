.DS_Store
node_modules/
*.env
.env
.venv

# Khảo sát: CSV thô từ Google Form chứa họ tên đầy đủ + mã HV — không commit
*Form Responses*.csv
evidence/survey/raw/
__pycache__/

# Slide đã tách từ capture: mang watermark email cá nhân + nội dung bài giảng
# thuộc quy định bảo mật data pack → KHÔNG commit (README mục "Bảo mật").
codebase/data/slides/

# Trace từ test offline (mock) sinh lại mỗi lần chạy — không cần vào repo.
# Trace từ AI THẬT (live-*) thì GIỮ: rubric R5 cần log/trace trong repo.
codebase/logs/traces/*-test-*.jsonl

# Cache recap: nội dung dẫn xuất từ data pack — không commit (sinh lại bằng app).
codebase/data/recap-cache/

# venv — dựng lại bằng: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
.venv/
